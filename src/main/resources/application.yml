spring:
  datasource:
    url: jdbc:h2:file:./data/webhookdb;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
app:
  generate-webhook-url: "https://bfhldevapigw.healthrx.co.in/hiring/generateWebhook/JAVA"
  test-webhook-url: "https://bfhldevapigw.healthrx.co.in/hiring/testWebhook/JAVA"
  # The regNo we will register with the generateWebhook call
  regNo: "REG12347"
  name: "John Doe"
  email: "john@example.com"
